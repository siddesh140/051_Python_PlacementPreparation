# How to unpack List.

# The * (called unpacking operator) takes the list and unpacks its elements one by one.
# So instead of passing the list as a single object, each element is passed separately to print().

a = [1, 2, 3, 4, 5]
print(*a)
